[package]
name    = "hello-plugin"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-sdk = { path = "../../crates/plugin-sdk" }
