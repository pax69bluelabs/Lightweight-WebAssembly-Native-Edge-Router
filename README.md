# Lightweight-WebAssembly-Native-Edge-Router
The Problem: Traditional edge proxies carry heavy footprints and lack programmatic flexibility at the absolute closest point to the user.  The Project: An ultra-fast, zero-dependency edge router built in Rust that executes lightweight WASM modules for dynamic request manipulation, header injection, and localized caching.  
